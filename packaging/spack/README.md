# Mneme Spack Package

Add the repository:

```bash
spack repo add packaging/spack/spack_repo/mneme
```

Install the latest develop commit with HIP or CUDA support:

```bash
spack install mneme@develop +hip
spack install mneme@develop +cuda
```
