.base-job:
  extends: .job_on_tioga
  interruptible: true
  before_script:
    - echo "=== Start CI Spack Tests ==="
  after_script:
    - !reference [.clean-flux-allocation, after_script]
    - !reference [.remove-builds-dir, after_script]
    - echo "=== End CI Spack Tests ==="

.variants:
  parallel:
    matrix:
      - MNEME_CI_ROCM_VERSION: ["6.4.3", "7.1.1", "7.2.0"]

test-spack-tioga:
  extends: [.base-job, .variants]
  variables:
    MNEME_CI_AMDGPU_TARGET: "gfx90a"
